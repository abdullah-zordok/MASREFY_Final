# Masarifi API

Implemented NestJS modular monolith with separate API, worker and migration entry points. Copy `.env.example`, install with `npm ci --ignore-scripts`, and run `npm run verify`. Docker is required for the Supabase/database and release-image scripts.
