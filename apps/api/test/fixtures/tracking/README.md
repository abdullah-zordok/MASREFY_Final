# Phase 08 fictional fixtures

These fixtures were authored for SPEC-BE-008 on 2026-09-02. They contain no real
institution, sender, customer, or transaction data. Each file has one literal
purpose: `valid.csv` parses two rows, `formula.csv` is rejected before parsing,
and `parser-corpus.json` proves one bounded data-only parser result.
